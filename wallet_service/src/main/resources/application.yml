server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paypal_wallet
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect